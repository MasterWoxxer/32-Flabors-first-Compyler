# ── Provider API keys ────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=

# Optional — only needed when swapping the labor model via the UI toggle
OPENAI_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# ── Supabase (Phase 2 — leave blank until Supabase project is created) ───────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── Python service URL (defaults to http://localhost:8000) ──────────────────
PYTHON_SERVICE_URL=http://localhost:8000
